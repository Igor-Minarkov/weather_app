export const metadata = {
  title: "Weather Dashboard",
  description: "A simple weather dashboard using the Next.js App Router",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
